# Runtime dependencies for auton-agent.
# Deliberately minimal: the agent shell-outs to the OS for heavy lifting
# instead of pulling in a large dependency tree (keeps the image small and
# the cold start on Render Free fast).
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
httpx>=0.27,<1.0

# Development / test only
pytest>=8.0
